const initialState = {
    isLoading: false,
    error: false,
    data: {}
};

const recoverState = (state: Object = initialState, action: Object) => {
    return state;
};

export default recoverState;
