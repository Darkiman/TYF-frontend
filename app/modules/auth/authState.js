const initialState = {
    isLoading: false,
    error: false,
};

const authState = (state: Object = initialState, action: Object) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};

export default authState;
