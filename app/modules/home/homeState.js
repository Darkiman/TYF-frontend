const initialState = {
  data: {},
  isLoading: false,
  error: false,
};


const homeState = (state: Object = initialState, action: Object) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default homeState;
