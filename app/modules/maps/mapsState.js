const initialState = {
  data: {},
  isLoading: false,
  error: false,
};


const mapsState = (state: Object = initialState, action: Object) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default mapsState;
