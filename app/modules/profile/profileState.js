const initialState = {
  data: {},
  isLoading: false,
  error: false,
};


const profileState = (state: Object = initialState, action: Object) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default profileState;
