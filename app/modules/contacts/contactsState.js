const initialState = {
  data: {},
  isLoading: false,
  error: false,
};


const contactsState = (state: Object = initialState, action: Object) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default contactsState;
