const initialState = {
  data: {},
  isLoading: false,
  error: false,
};


const settingsState = (state: Object = initialState, action: Object) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default settingsState;
