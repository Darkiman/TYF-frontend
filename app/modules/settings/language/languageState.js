const initialState = {
  data: {},
  isLoading: false,
  error: false,
};


const languageState = (state: Object = initialState, action: Object) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default languageState;
